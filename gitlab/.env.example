
# Hostname interno do container (pode ser o mesmo nome)
GITLAB_HOSTNAME=localhost:8080

# URLs externas (ajusta para o teu domínio/IP)
GITLAB_EXTERNAL_URL=http://localhost:8080

# Ports expostos no host
GITLAB_HTTP_PORT=8080
GITLAB_HTTPS_PORT=8443
GITLAB_SSH_PORT=2222

# Registry (opcional)
GITLAB_REGISTRY_EXTERNAL_URL=http://localhost:5050
GITLAB_REGISTRY_PORT=5050